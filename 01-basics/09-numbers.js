const num = 200
console.log(num.toString().length);


const num2 = new Number(num)
console.log(num2);
//now the num2 is object, we can perform any function like tostring, tofixed
//toPrecision

//ceil, floor, min, max, round, random
 